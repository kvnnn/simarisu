drop table if exists card;
create table card(id integer, name text, description text, sprite text, effect text, rarity integer, type integer, damage integer, range_type integer, range_size real);
insert into card values(0, 'attack', 'normal attack', '1', '', 1, 1, 0, 0, 0.25);
