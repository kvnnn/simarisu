drop table if exists user;
create table user(id integer primary key, sprite text, point integer, hp integer, damage integer, cure integer, move integer);
insert into user(sprite, point, hp, damage, cure, move)
	values('0', 0, 10, 0, 0, 3);
